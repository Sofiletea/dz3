package main

import "fmt"

func main() {
	var x int = 1
	var y int = 4
	fmt.Println(x, y)
}

func sum(x, y int) (int, int) {
	return x + y, x - y
	//return x + y Функция, возвращающая значение: Создайте функцию, которая принимает два

}
